# Supported environment: CPython 3.12+ (validated on 3.14, Linux).
# These are direct dependency pins. A complete transitive lock is generated
# from a clean, successfully validated virtual environment.
# Test tooling (pytest) is provided by the package dev extra: pip install -e ".[dev]"
# Optional: topics 13-17 library-comparison notebooks additionally need torch
# (install separately: pip install torch).

# Numerical computing and data
numpy==2.4.6
pandas==3.0.3
scipy==1.17.1

# Visualization and classical machine learning
matplotlib==3.10.9
scikit-learn==1.9.0

# Jupyter authoring and programmatic execution
jupyterlab==4.5.8
ipykernel==7.2.0
ipywidgets==8.1.8
nbformat==5.10.4
nbclient==0.11.0
