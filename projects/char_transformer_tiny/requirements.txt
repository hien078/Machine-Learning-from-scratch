# No dependencies beyond the repo root environment (see ../../requirements.txt).
# Uses only: numpy (model + training), pytest (tests, via the root dev extra).
# Install from the repo root:  pip install -r requirements.txt && pip install -e ".[dev]"
