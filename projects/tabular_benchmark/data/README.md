# data/

Intentionally empty. Both benchmark datasets are scikit-learn **built-ins**
(`sklearn.datasets.load_diabetes`, `sklearn.datasets.load_breast_cancer`)
shipped inside the scikit-learn package — nothing is downloaded and nothing is
stored in this directory.
