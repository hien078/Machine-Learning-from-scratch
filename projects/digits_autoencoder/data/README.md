# Data

Nothing is stored here. The experiment uses sklearn's bundled
`sklearn.datasets.load_digits` (1797 samples of 8x8 grayscale digits, a subset of
the UCI Optical Recognition of Handwritten Digits dataset), which ships inside the
scikit-learn package itself — no download, no cache, no files.
