# No additional dependencies. This project runs entirely on the repository's
# root environment (numpy, scikit-learn, matplotlib, and the editable-installed
# ml_first_principles package). From the repo root:
#
#   source .venv/bin/activate          # or create it per the root README
#   pip install -r requirements.txt    # root pins
#   pip install -e ".[dev]"            # ml_first_principles + pytest/ruff
-r ../../requirements.txt
